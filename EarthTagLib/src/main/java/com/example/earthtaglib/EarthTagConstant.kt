package com.example.earthtaglib

/**
 * @author kun
 * @since 5/15/21
 */
/**
 * 旋转单位，默认为一度,弧度制
 */
const val ROTATE_DEGREE_UNIT = Math.PI / 360
const val DEFAULT_RADIUS = 300
const val DEFAULT_PERCENT = 0.9f
const val DEFAULT_DELTA_SCALE = 1.5f
const val DEFAULT_MIN_ALPHA = 0f
const val DEFAULT_MAX_ALPHA = 0.7f
const val DEFAULT_MIN_VELOCITY = 0.6f
const val DEFAULT_MAX_VELOCITY = 1.2f

val DEFAULT_COLOR_LIGHT = floatArrayOf(0.886f, 0.725f, 0.188f, 1f)
val DEFAULT_COLOR_DARK = floatArrayOf(0.3f, 0.3f, 0.3f, 1f)
